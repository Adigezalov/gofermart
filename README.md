# Гофермарт - Накопительная система лояльности

Система лояльности для интернет-магазина с возможностью регистрации пользователей, загрузки заказов и начисления баллов.

## Возможности

- ✅ Регистрация пользователей
- ✅ Авторизация пользователей
- ✅ JWT токены (access + refresh)
- ✅ Middleware для проверки авторизации
- ✅ Middleware для логирования запросов
- ✅ Защищенные эндпоинты
- ✅ Автоматическое обновление refresh токенов
- ✅ Автоматические миграции базы данных
- ✅ Health check эндпоинты
- ✅ Алгоритм Луна для валидации номеров заказов
- ✅ Загрузка заказов пользователями
- ✅ Получение списка заказов
- ✅ Система начисления баллов
- ✅ Получение баланса пользователя
- ✅ Списание баллов
- ✅ История операций списания
- ✅ Интеграция с внешней системой начислений

## Быстрый старт

### 1. Установка зависимостей
```bash
make deps
```

### 2. Настройка базы данных
Создайте базу данных PostgreSQL и настройте подключение через переменные окружения:

```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

### 3. Запуск системы
```bash
# Рекомендуется: запуск полной системы (gophermart + accrual)
make run-full

# Или только основного сервера
make run
```

Миграции применятся автоматически при запуске сервера.
- Gophermart будет доступен по адресу: http://localhost:8080
- Accrual сервис будет доступен по адресу: http://localhost:8081

**Как работает система начислений:**
1. Пользователь загружает заказ → сохраняется со статусом NEW
2. Внешняя система начислений самостоятельно обрабатывает заказы
3. Accrual worker каждые 30 секунд проверяет статус заказов во внешней системе
4. При изменении статуса → обновляется в нашей БД и начисляются баллы (если есть)

Для остановки всех процессов:
```bash
make stop-all
```

## Конфигурация

Приложение настраивается через переменные окружения или флаги командной строки:

| Переменная окружения | Флаг | По умолчанию | Описание |
|---------------------|------|--------------|----------|
| `RUN_ADDRESS` | `-a` | `:8080` | Адрес и порт запуска сервиса |
| `DATABASE_URI` | `-d` | `postgres://...` | Строка подключения к PostgreSQL |
| `ACCRUAL_SYSTEM_ADDRESS` | `-r` | `http://localhost:8081` | Адрес системы начислений |
| `JWT_SECRET` | `-jwt-secret` | `your-secret-key...` | Секретный ключ для JWT |

## API Эндпоинты

### Health Check
- `GET /api/health/check` - Проверка работоспособности сервиса
- `GET /api/health/db` - Проверка подключения к базе данных
- `GET /api/health/auth` - Проверка авторизации (требует Bearer токен)

### Пользователи
- `POST /api/user/register` - Регистрация пользователя
- `POST /api/user/login` - Авторизация пользователя

### Заказы
- `POST /api/user/orders` - Загрузка номера заказа (требует авторизации)
- `GET /api/user/orders` - Получение списка заказов пользователя (требует авторизации)

### Баланс и начисления
- `GET /api/user/balance` - Получение текущего баланса пользователя (требует авторизации)
- `POST /api/user/balance/withdraw` - Списание баллов с баланса (требует авторизации)
- `GET /api/user/withdrawals` - Получение истории списаний (требует авторизации)

#### Пример регистрации:
```bash
curl -X POST http://localhost:8080/api/user/register \
  -H "Content-Type: application/json" \
  -d '{"login": "testuser", "password": "password123"}'
```

#### Пример авторизации:
```bash
curl -X POST http://localhost:8080/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"login": "testuser", "password": "password123"}'
```

#### Пример использования защищенного эндпоинта:
```bash
# Сначала получите токен через авторизацию, затем:
curl -X GET http://localhost:8080/api/health/auth \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### Пример загрузки заказа:
```bash
# Загрузка номера заказа (Content-Type: text/plain)
curl -X POST http://localhost:8080/api/user/orders \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: text/plain" \
  -d "79927398713"
```

#### Пример получения списка заказов:
```bash
# Получение списка заказов пользователя
curl -X GET http://localhost:8080/api/user/orders \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

Ответ (при наличии заказов):
```json
[
  {
    "number": "79927398713",
    "status": "NEW",
    "uploaded_at": "2025-08-11T17:46:22.33442Z"
  },
  {
    "number": "49927398716", 
    "status": "PROCESSED",
    "accrual": 500.0,
    "uploaded_at": "2025-08-11T17:40:15.12345Z"
  }
]
```

Возможные статусы заказов:
- `NEW` - заказ загружен в систему, но не попал в обработку
- `PROCESSING` - вознаграждение за заказ рассчитывается
- `INVALID` - система расчёта вознаграждений отказала в расчёте
- `PROCESSED` - данные по заказу проверены и информация о расчёте успешно получена

#### Пример получения баланса:
```bash
# Получение текущего баланса пользователя
curl -X GET http://localhost:8080/api/user/balance \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

Ответ:
```json
{
  "current": 500.5,
  "withdrawn": 42.0
}
```

#### Пример списания баллов:
```bash
# Списание баллов за заказ
curl -X POST http://localhost:8080/api/user/balance/withdraw \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"order": "79927398713", "sum": 100}'
```

#### Пример получения истории списаний:
```bash
# Получение истории операций списания
curl -X GET http://localhost:8080/api/user/withdrawals \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

Ответ:
```json
[
  {
    "order": "79927398713",
    "sum": 100,
    "processed_at": "2025-08-11T18:25:37Z"
  }
]
```

## Команды Make

### Основные команды
- `make run-full` - **Запустить полную систему** (gophermart + accrual) 🚀
- `make test-system` - **Быстрый тест работоспособности** системы
- `make stop-all` - Остановить все процессы
- `make build` - Собрать приложение
- `make help` - Показать справку

### Отдельные сервисы
- `make run` - Запустить только gophermart
- `make run-accrual` - Запустить только accrual сервис
- `make run-env` - Запустить gophermart с переменными из .env
- `make run-full-env` - Запустить полную систему с переменными из .env

### Миграции и утилиты
- `make migrate` - Применить миграции вручную
- `make migrate-status` - Показать статус миграций
- `make deps` - Установить зависимости
- `make clean` - Очистить собранные файлы

## Структура проекта

```
├── cmd/
│   ├── gophermart/     # Основное приложение
│   └── migrate/        # Утилита для миграций
├── internal/
│   ├── config/         # Конфигурация
│   ├── health/         # Health checks
│   ├── migrations/     # Система миграций
│   ├── tokens/         # JWT токены
│   ├── user/           # Пользователи
│   └── ...
├── migrations/         # SQL миграции
├── http-client/        # HTTP тесты
└── Makefile           # Команды сборки
```

## Разработка

### Тестирование API

**🎯 Рекомендуется:** `http-client/complete-loyalty-system.http` - полный цикл тестирования всей системы с автоматическим сохранением токенов.

📖 **Быстрый старт:** `http-client/QUICK-START.md`

Отдельные тесты в папке `http-client/`:
- `http-client/auth/` - тесты аутентификации и авторизации
- `http-client/health/` - тесты health check эндпоинтов  
- `http-client/orders/` - тесты работы с заказами
- `http-client/balance/` - тесты системы начисления баллов
- `http-client/utils/` - утилиты и вспомогательные тесты

Подробности в `http-client/README.md`.


### Логирование
Все HTTP запросы автоматически логируются с информацией:
- Метод и URL запроса
- Статус код ответа (с цветовой индикацией)
- Время выполнения
- IP адрес клиента
- Размер ответа
- Content-Type (для POST/PUT запросов)

Пример лога:
```
GET /api/health/check 200 95.75µs [::1]:63975 16 bytes
POST /api/user/register 409 4.353041ms [::1]:63979 29 bytes [application/json]
```

### Миграции
Миграции применяются автоматически при запуске сервера. Для ручного управления используйте:

```bash
# Проверить статус
make migrate-status

# Применить миграции
make migrate
```

### Алгоритм Луна
Для валидации номеров заказов используется алгоритм Луна. Утилита для тестирования:

```bash
# Проверить номер
go run cmd/luhn-generator/main.go validate 79927398713

# Сгенерировать валидный номер
go run cmd/luhn-generator/main.go generate 123456789
```